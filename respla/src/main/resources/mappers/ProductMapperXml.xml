<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.res.pla.mapper.ProductMapper">
	
	    <select id="selectPtypeProducts" resultType="com.res.pla.domain.ProductDTO">
	        SELECT * FROM product where ptype = #{ptype}
	    </select>
	    
	     <select id="selectAllProducts" resultType="com.res.pla.domain.ProductDTO" >
	        SELECT * FROM product
	    </select>
	    
	    <select id="selectProduct" resultType="com.res.pla.domain.ProductDTO" >
	        SELECT * FROM product WHERE productcode = #{productcode}
	    </select>
	    
	   	<insert id="purchaseProduct" parameterType="map" keyProperty="uppcode">
	   		<selectKey keyProperty="uppcode" resultType="String" order="BEFORE">
        	SELECT concat(#{id}, #{productcode}, (SELECT ptype FROM product WHERE productcode = #{productcode}),
                      substring(date_format(now(), '%Y%m%d%H%i%s'), 3)) AS uppcode
   		 	</selectKey>
			insert into userpurchasedproduct(uppcode , id , productcode , ptype , initialtimevalue , availabletime ,
			startdate , initialdayvalue , availableday , enddate , price , usable , payment)
			values(
			#{uppcode},
			#{id},
			#{productcode},
			(select ptype from product where productcode=#{productcode}),
			(select timevalue from product where productcode=#{productcode}),
			(select timevalue from product where productcode=#{productcode}),
			#{startDateTime},
			(select dayvalue from product where productcode=#{productcode}),
			(select dayvalue from product where productcode=#{productcode}),
			#{endDateTime},
			(select price from product where productcode=#{productcode}),
			#{usable},
			#{paymentOption}
		);
		</insert>
		
		<update id="updateSellCount">
		update product set sellcount = sellcount + #{amount} where productcode = #{productcode}
		</update>
	    
	    <update id="updateRefundCount">
		update product set refundcount = refundcount + #{amount} where productcode = #{productcode}
		</update>
		
	</mapper>
	
