<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.res.pla.mapper.UsageHistoryMapper">
	
	   	<insert id="recordAction" parameterType="map" keyProperty="uppcode">
	   		<selectKey keyProperty="uhcode" resultType="String" order="BEFORE">
        	select concat(#{id}, #{productcode}, (SELECT ptype FROM product WHERE productcode = #{productcode}),
                      substring(date_format(now(), '%Y%m%d%H%i%s'), 3)) AS uppcode
   		 	</selectKey>
			insert into userpurchasedproduct(uppcode , id , productcode , ptype , initialtimevalue , availabletime ,
			startdate , initialdayvalue , availableday , enddate , price , usable , payment)
			values(
			#{uppcode},
			#{id},
			#{productcode},
			(select ptype from product where productcode=#{productcode}),
			(select timevalue from product where productcode=#{productcode}),
			(select timevalue from product where productcode=#{productcode}),
			#{startDateTime},
			(select dayvalue from product where productcode=#{productcode}),
			(select dayvalue from product where productcode=#{productcode}),
			#{endDateTime},
			(select price from product where productcode=#{productcode}),
			#{usable},
			#{paymentOption}
		);
		</insert>
		
		
	    <select id="selectAllHistory" resultType="com.res.pla.domain.UsageHistoryDTO">
	        SELECT * FROM usagehistory
	    </select>
	    
	    <select id="selectAllHistoryById" resultType="com.res.pla.domain.UsageHistoryDTO" >
	        SELECT * FROM usagehistory where id = #{id}
	    </select>
	    
	     <select id="selectAllHistoryByIdActionType" resultType="com.res.pla.domain.UsageHistoryDTO" >
	        SELECT * FROM usagehistory where id = #{id}
	    </select>
		
	</mapper>
	
